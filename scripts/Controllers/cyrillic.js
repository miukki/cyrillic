define(['jquery', 'Views/view', 'Models/model'], function($){

    function init(){
      //var input = $('input' +'[data-input-view=\'cyrillic\']');

    }


    return {
        init:init
    };
});
